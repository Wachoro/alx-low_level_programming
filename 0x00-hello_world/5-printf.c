#include <stdio.h>
/**
 * main - A program that prints a line using the printf function
 */
int main(void)
{
	/* the printf function prints the string*/
	printf("with proper grammar, but the outcome is a piece of art,";
		return (0);
}
